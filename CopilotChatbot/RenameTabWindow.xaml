<Window x:Class="CopilotChatbot.RenameTabWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        Title="Rename Tab" Height="186" Width="440" ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner" Background="#F3F5F8">
    <Window.Resources>
        <SolidColorBrush x:Key="SurfaceBrush" Color="#FFFFFF" />
        <SolidColorBrush x:Key="ControlBrush" Color="#F8FAFC" />
        <SolidColorBrush x:Key="BorderBrushModern" Color="#D0D7DE" />
        <SolidColorBrush x:Key="TextBrush" Color="#1F2328" />
        <SolidColorBrush x:Key="MutedTextBrush" Color="#5D6673" />
        <SolidColorBrush x:Key="AccentBrush" Color="#0A65CC" />
    </Window.Resources>
    <DockPanel>
        <!-- Footer -->
        <Border DockPanel.Dock="Bottom" Background="{StaticResource SurfaceBrush}"
                BorderBrush="{StaticResource BorderBrushModern}" BorderThickness="0,1,0,0" Padding="16,12">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Width="84" Margin="0,0,8,0" Click="Cancel_Click">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border x:Name="Chr" Background="{StaticResource ControlBrush}"
                                    BorderBrush="{StaticResource BorderBrushModern}" BorderThickness="1" CornerRadius="8">
                                <TextBlock Text="Cancel" Foreground="{StaticResource TextBrush}"
                                           FontWeight="SemiBold" FontSize="13"
                                           HorizontalAlignment="Center" VerticalAlignment="Center" Margin="0,8" />
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="Chr" Property="Background" Value="{StaticResource SurfaceBrush}" />
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Button.Template>
                </Button>
                <Button Width="84" Click="Rename_Click">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border x:Name="Chr" Background="{StaticResource AccentBrush}"
                                    BorderBrush="{StaticResource AccentBrush}" BorderThickness="1" CornerRadius="8">
                                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center"
                                            VerticalAlignment="Center" Margin="0,8">
                                    <ui:SymbolIcon Symbol="Rename20" FontSize="14" Foreground="White" Margin="0,0,5,0" />
                                    <TextBlock Text="Rename" Foreground="White" FontWeight="SemiBold" FontSize="13" />
                                </StackPanel>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="Chr" Property="Background" Value="#0856B3" />
                                </Trigger>
                                <Trigger Property="IsPressed" Value="True">
                                    <Setter TargetName="Chr" Property="Background" Value="#0648A0" />
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Button.Template>
                </Button>
            </StackPanel>
        </Border>

        <!-- Content -->
        <StackPanel Margin="18,16,18,0">
            <TextBlock Text="Tab name" FontWeight="SemiBold" FontSize="13"
                       Foreground="{StaticResource TextBrush}" Margin="0,0,0,7" />
            <TextBox x:Name="TitleTextBox" Height="34" Padding="9,0"
                     Background="{StaticResource ControlBrush}" Foreground="{StaticResource TextBrush}"
                     BorderBrush="{StaticResource BorderBrushModern}" BorderThickness="1"
                     VerticalContentAlignment="Center" />
        </StackPanel>
    </DockPanel>
</Window>
